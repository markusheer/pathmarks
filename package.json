{
	"name": "pathmarks",
	"repository": {
		"type": "git",
		"url": "https://github.com/markusheer/pathmarks.git"
	},
	"license": "MIT",
	"scripts": {
		"clean": "rimraf dist build",
		"start": "webpack --mode=development --watch --progress --profile --colors",
		"build": "webpack --mode=development --progress --profile --colors",
		"dist": "webpack --mode=production --progress --profile --colors"
	},
	"devDependencies": {
		"generate-json-file-webpack-plugin": "0.0.3",
		"rimraf": "^2.6.2",
		"webpack": "^4.15.1",
		"webpack-cli": "^3.1.0",
		"zip-webpack-plugin": "^3.0.0",
		"copy-webpack-plugin": "^4.5.2"
	},
	"dependencies": {
		"material-design-icons": "^3.0.1",
		"sortablejs": "^1.7.0"
	}
}
